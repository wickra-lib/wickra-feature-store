# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_feature_store.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKFEATURESTORE_INC)
PKG_LIBS = -L$(WKFEATURESTORE_LIB) -l:wickra_feature_store.dll
